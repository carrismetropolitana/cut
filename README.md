# USER PORTAL

To start local development, the following lines must be added to the `hosts` file:

run `sudo nano /etc/hosts`

```
127.0.0.1 {the fare-engine staging URL}
127.0.0.1 {the fare-engine production URL}
```